# 从账户中心安全渠道拿到的验签密钥
JWT_SECRET=在这里粘贴你的JWT_SECRET

# 播放数据静态加密主密钥（32字节hex）。建议用 openssl rand -hex 32 生成
DATA_ENC_KEY=

# 会话签名密钥。同样建议用 openssl rand -hex 32 生成
SESSION_SECRET=换成你自己的随机串

# 生产环境务必设为 true（走 https），本地开发不要设或设 false
COOKIE_SECURE=true
